---
date: 2026-08-03
type: entry
corrects: null
source: typed
projects: [icm-journal]
minutes: {icm-journal: 55}
energy: 4
blockers:
  - id: schema-churn
    project: icm-journal
    note: rewrote the frontmatter fields a third time instead of writing entries with them
shipped:
  - project: icm-journal
    what: entry schema doc committed, blocker ids specified as the recurrence key
mood_note: good session, felt like the design finally clicked
---

Sat down at 8pm as planned. Spent most of it on the schema rather than on the pipeline, which was not the plan, but the blocker-id decision felt worth the time - without a stable id there is no way to detect a recurring blocker except fuzzy matching prose, which will not work.

Kept catching myself wanting to add fields. Cut `tags` and `location` before they got in. The rule I am holding myself to: if the review does not count it, it does not go in frontmatter.

Tomorrow: actually write the weekly stage contract. No more schema.
